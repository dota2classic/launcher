<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:d2c_launcher.ViewModels"
             xmlns:l="clr-namespace:d2c_launcher.Util"
             x:Class="d2c_launcher.Views.Components.StorePanel"
             x:DataType="vm:StoreViewModel">

  <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
    <StackPanel Spacing="2">

      <!-- ═══ HERO BANNER ═══ -->
      <Panel MinHeight="220">
        <!-- Background gradient -->
        <Border>
          <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
              <GradientStop Color="#09090b" Offset="0"/>
              <GradientStop Color="#161208" Offset="0.6"/>
              <GradientStop Color="#1a1508" Offset="1"/>
            </LinearGradientBrush>
          </Border.Background>
        </Border>

        <!-- Hero image (right side, clipped) -->
        <Border HorizontalAlignment="Right" Width="380" ClipToBounds="True" Opacity="0.55">
          <Image Source="avares://d2c-launcher/Assets/Images/splash/subscription.webp"
                 Stretch="UniformToFill" HorizontalAlignment="Center"/>
        </Border>

        <!-- Right-side fade overlay so image blends into background -->
        <Border HorizontalAlignment="Right" Width="200">
          <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
              <GradientStop Color="#00161208" Offset="0"/>
              <GradientStop Color="#FF161208" Offset="1"/>
            </LinearGradientBrush>
          </Border.Background>
        </Border>
        <!-- Left-side fade overlay -->
        <Border HorizontalAlignment="Right" Width="380">
          <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
              <GradientStop Color="#FF09090b" Offset="0"/>
              <GradientStop Color="#00090000" Offset="0.45"/>
            </LinearGradientBrush>
          </Border.Background>
        </Border>

        <!-- Text content -->
        <StackPanel VerticalAlignment="Center" Margin="44,36">

          <!-- Title -->
          <StackPanel Orientation="Horizontal" Spacing="16" Margin="0,0,0,12">
            <TextBlock Text="✦" Foreground="#C8A84B"
                       FontSize="{DynamicResource FontSize4XL}" VerticalAlignment="Center"/>
            <TextBlock Text="DOTACLASSIC PLUS" Foreground="#C8A84B"
                       FontSize="{DynamicResource FontSize4XL}" FontWeight="Bold"
                       LetterSpacing="4" VerticalAlignment="Center"/>
          </StackPanel>

          <!-- Tagline -->
          <TextBlock Text="{l:T 'store.heroDesc'}"
                     Foreground="#8090A4" FontSize="{DynamicResource FontSizeLG}"
                     TextWrapping="Wrap" MaxWidth="480" Margin="0,0,0,24"/>

          <!-- Loading -->
          <TextBlock IsVisible="{Binding IsLoading}"
                     Text="{l:T 'profile.loading'}"
                     Foreground="#556070" FontSize="{DynamicResource FontSizeMD}"
                     Margin="0,0,0,12"/>

          <!-- Active subscription -->
          <StackPanel IsVisible="{Binding HasPlusSubscription}" Spacing="6" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal" Spacing="10">
              <TextBlock Text="✦" Foreground="#C8A84B"
                         FontSize="{DynamicResource FontSizeMD}" VerticalAlignment="Center"/>
              <TextBlock Text="{l:T 'store.subscriptionActive'}" Foreground="#C8A84B"
                         FontSize="{DynamicResource FontSizeMD}" FontWeight="SemiBold"
                         VerticalAlignment="Center"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Spacing="8">
              <TextBlock Text="{l:T 'store.subscriptionExpires'}"
                         Foreground="#556070" FontSize="{DynamicResource FontSizeMD}"/>
              <TextBlock Text="{Binding PlusSubscriptionEndText}"
                         Foreground="#99A8B4" FontSize="{DynamicResource FontSizeMD}"
                         FontWeight="SemiBold"/>
            </StackPanel>
          </StackPanel>

          <!-- No subscription -->
          <TextBlock IsVisible="{Binding !HasPlusSubscription}"
                     Text="{l:T 'store.subscriptionNone'}"
                     Foreground="#556070" FontSize="{DynamicResource FontSizeMD}"
                     Margin="0,0,0,20"/>

          <!-- CTA button -->
          <Button Classes="PrimaryButton" Command="{Binding OpenTelegramCommand}"
                  IsVisible="{Binding !IsLoading}"
                  Padding="22,10" CornerRadius="2" Cursor="Hand"
                  HorizontalAlignment="Left">
            <StackPanel Orientation="Horizontal" Spacing="10">
              <Image Source="avares://d2c-launcher/Assets/Images/telegram.png"
                     Width="16" Height="16" VerticalAlignment="Center"/>
              <TextBlock Text="{Binding StoreButtonText}"
                         FontSize="{DynamicResource FontSizeMD}" FontWeight="Bold"/>
            </StackPanel>
          </Button>
        </StackPanel>
      </Panel>

      <!-- ─── Section divider ─── -->
      <Border Background="#0d0f13" Padding="44,14">
        <TextBlock Text="{l:T 'store.benefitsTitle'}"
                   Foreground="#3a4860" FontSize="{DynamicResource FontSizeXS}"
                   FontWeight="SemiBold" LetterSpacing="3"/>
      </Border>

      <!-- ═══ FEATURE 1 — text RIGHT, image full-bleed (decorations) ═══ -->
      <Panel MinHeight="260" ClipToBounds="True">
        <Image Source="avares://d2c-launcher/Assets/Images/splash/decoration.webp"
               Stretch="UniformToFill" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <!-- gradient: transparent left → dark right -->
        <Border>
          <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
              <GradientStop Color="#000e1016" Offset="0.20"/>
              <GradientStop Color="#880e1016" Offset="0.48"/>
              <GradientStop Color="#D80e1016" Offset="0.66"/>
              <GradientStop Color="#F20e1016" Offset="1"/>
            </LinearGradientBrush>
          </Border.Background>
        </Border>
        <Grid ColumnDefinitions="0.42*,0.58*">
          <Grid Grid.Column="1" ColumnDefinitions="Auto,*" VerticalAlignment="Center" Margin="40,32,44,32">
            <TextBlock Grid.Column="0" Text="{l:T 'store.benefit1Title'}"
                       Foreground="#E8ECF0" FontSize="{DynamicResource FontSize2XL}"
                       FontWeight="Bold" TextWrapping="Wrap"
                       VerticalAlignment="Center" Margin="0,0,28,0"/>
            <TextBlock Grid.Column="1" Text="{l:T 'store.benefit1Desc'}"
                       Foreground="#8898B8" FontSize="{DynamicResource FontSizeMD}"
                       TextWrapping="Wrap" VerticalAlignment="Center"/>
          </Grid>
        </Grid>
      </Panel>

      <!-- ═══ FEATURE 2 — text LEFT, image full-bleed (dodge) ═══ -->
      <Panel MinHeight="260" ClipToBounds="True">
        <Image Source="avares://d2c-launcher/Assets/Images/splash/dodge.webp"
               Stretch="UniformToFill" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <!-- gradient: dark left → transparent right -->
        <Border>
          <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
              <GradientStop Color="#F20b0d11" Offset="0"/>
              <GradientStop Color="#D80b0d11" Offset="0.34"/>
              <GradientStop Color="#880b0d11" Offset="0.52"/>
              <GradientStop Color="#000b0d11" Offset="0.80"/>
            </LinearGradientBrush>
          </Border.Background>
        </Border>
        <Grid ColumnDefinitions="0.58*,0.42*">
          <Grid Grid.Column="0" ColumnDefinitions="Auto,*" VerticalAlignment="Center" Margin="44,32,40,32">
            <TextBlock Grid.Column="0" Text="{l:T 'store.benefit2Title'}"
                       Foreground="#E8ECF0" FontSize="{DynamicResource FontSize2XL}"
                       FontWeight="Bold" TextWrapping="Wrap"
                       VerticalAlignment="Center" Margin="0,0,28,0"/>
            <TextBlock Grid.Column="1" Text="{l:T 'store.benefit2Desc'}"
                       Foreground="#8898B8" FontSize="{DynamicResource FontSizeMD}"
                       TextWrapping="Wrap" VerticalAlignment="Center"/>
          </Grid>
        </Grid>
      </Panel>

      <!-- ═══ FEATURE 3 — text RIGHT, image full-bleed (unban) ═══ -->
      <Panel MinHeight="260" ClipToBounds="True">
        <Image Source="avares://d2c-launcher/Assets/Images/splash/unban.webp"
               Stretch="UniformToFill" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <Border>
          <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
              <GradientStop Color="#000e1016" Offset="0.20"/>
              <GradientStop Color="#880e1016" Offset="0.48"/>
              <GradientStop Color="#D80e1016" Offset="0.66"/>
              <GradientStop Color="#F20e1016" Offset="1"/>
            </LinearGradientBrush>
          </Border.Background>
        </Border>
        <Grid ColumnDefinitions="0.42*,0.58*">
          <Grid Grid.Column="1" ColumnDefinitions="Auto,*" VerticalAlignment="Center" Margin="40,32,44,32">
            <TextBlock Grid.Column="0" Text="{l:T 'store.benefit3Title'}"
                       Foreground="#E8ECF0" FontSize="{DynamicResource FontSize2XL}"
                       FontWeight="Bold" TextWrapping="Wrap"
                       VerticalAlignment="Center" Margin="0,0,28,0"/>
            <TextBlock Grid.Column="1" Text="{l:T 'store.benefit3Desc'}"
                       Foreground="#8898B8" FontSize="{DynamicResource FontSizeMD}"
                       TextWrapping="Wrap" VerticalAlignment="Center"/>
          </Grid>
        </Grid>
      </Panel>

      <!-- ═══ FEATURE 4 — text LEFT, image full-bleed (in-game praise) ═══ -->
      <Panel MinHeight="260" ClipToBounds="True">
        <Image Source="avares://d2c-launcher/Assets/Images/splash/cm_lina.webp"
               Stretch="UniformToFill" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <Border>
          <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
              <GradientStop Color="#F20b0d11" Offset="0"/>
              <GradientStop Color="#D80b0d11" Offset="0.34"/>
              <GradientStop Color="#880b0d11" Offset="0.52"/>
              <GradientStop Color="#000b0d11" Offset="0.80"/>
            </LinearGradientBrush>
          </Border.Background>
        </Border>
        <Grid ColumnDefinitions="0.58*,0.42*">
          <Grid Grid.Column="0" ColumnDefinitions="Auto,*" VerticalAlignment="Center" Margin="44,32,40,32">
            <TextBlock Grid.Column="0" Text="{l:T 'store.benefit4Title'}"
                       Foreground="#E8ECF0" FontSize="{DynamicResource FontSize2XL}"
                       FontWeight="Bold" TextWrapping="Wrap"
                       VerticalAlignment="Center" Margin="0,0,28,0"/>
            <TextBlock Grid.Column="1" Text="{l:T 'store.benefit4Desc'}"
                       Foreground="#8898B8" FontSize="{DynamicResource FontSizeMD}"
                       TextWrapping="Wrap" VerticalAlignment="Center"/>
          </Grid>
        </Grid>
      </Panel>

      <!-- ═══ FEATURE 5 — text RIGHT, image full-bleed (mute) ═══ -->
      <Panel MinHeight="260" ClipToBounds="True">
        <Image Source="avares://d2c-launcher/Assets/Images/splash/mute.webp"
               Stretch="UniformToFill" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <Border>
          <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
              <GradientStop Color="#000e1016" Offset="0.20"/>
              <GradientStop Color="#880e1016" Offset="0.48"/>
              <GradientStop Color="#D80e1016" Offset="0.66"/>
              <GradientStop Color="#F20e1016" Offset="1"/>
            </LinearGradientBrush>
          </Border.Background>
        </Border>
        <Grid ColumnDefinitions="0.42*,0.58*">
          <Grid Grid.Column="1" ColumnDefinitions="Auto,*" VerticalAlignment="Center" Margin="40,32,44,32">
            <TextBlock Grid.Column="0" Text="{l:T 'store.benefit5Title'}"
                       Foreground="#E8ECF0" FontSize="{DynamicResource FontSize2XL}"
                       FontWeight="Bold" TextWrapping="Wrap"
                       VerticalAlignment="Center" Margin="0,0,28,0"/>
            <TextBlock Grid.Column="1" Text="{l:T 'store.benefit5Desc'}"
                       Foreground="#8898B8" FontSize="{DynamicResource FontSizeMD}"
                       TextWrapping="Wrap" VerticalAlignment="Center"/>
          </Grid>
        </Grid>
      </Panel>

      <!-- ═══ FEATURE 6 — text LEFT, image full-bleed (lobby) ═══ -->
      <Panel MinHeight="260" ClipToBounds="True">
        <Image Source="avares://d2c-launcher/Assets/Images/splash/lobby.webp"
               Stretch="UniformToFill" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <Border>
          <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
              <GradientStop Color="#F20b0d11" Offset="0"/>
              <GradientStop Color="#D80b0d11" Offset="0.34"/>
              <GradientStop Color="#880b0d11" Offset="0.52"/>
              <GradientStop Color="#000b0d11" Offset="0.80"/>
            </LinearGradientBrush>
          </Border.Background>
        </Border>
        <Grid ColumnDefinitions="0.58*,0.42*">
          <Grid Grid.Column="0" ColumnDefinitions="Auto,*" VerticalAlignment="Center" Margin="44,32,40,32">
            <TextBlock Grid.Column="0" Text="{l:T 'store.benefit6Title'}"
                       Foreground="#E8ECF0" FontSize="{DynamicResource FontSize2XL}"
                       FontWeight="Bold" TextWrapping="Wrap"
                       VerticalAlignment="Center" Margin="0,0,28,0"/>
            <TextBlock Grid.Column="1" Text="{l:T 'store.benefit6Desc'}"
                       Foreground="#8898B8" FontSize="{DynamicResource FontSizeMD}"
                       TextWrapping="Wrap" VerticalAlignment="Center"/>
          </Grid>
        </Grid>
      </Panel>

      <!-- ═══ BOTTOM CTA ═══ -->
      <Border Padding="44,32">
        <Border.Background>
          <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#09090b" Offset="0"/>
            <GradientStop Color="#16120a" Offset="0.55"/>
            <GradientStop Color="#09090b" Offset="1"/>
          </LinearGradientBrush>
        </Border.Background>
        <StackPanel Spacing="14" HorizontalAlignment="Left">
          <TextBlock Text="{l:T 'store.ctaTitle'}"
                     Foreground="#E2E6EA" FontSize="{DynamicResource FontSize2XL}"
                     FontWeight="Bold"/>
          <Button Classes="PrimaryButton" Command="{Binding OpenTelegramCommand}"
                  IsVisible="{Binding !IsLoading}"
                  Padding="22,10" CornerRadius="2" Cursor="Hand"
                  HorizontalAlignment="Left">
            <StackPanel Orientation="Horizontal" Spacing="10">
              <Image Source="avares://d2c-launcher/Assets/Images/telegram.png"
                     Width="16" Height="16" VerticalAlignment="Center"/>
              <TextBlock Text="{Binding StoreButtonText}"
                         FontSize="{DynamicResource FontSizeMD}" FontWeight="Bold"/>
            </StackPanel>
          </Button>
        </StackPanel>
      </Border>

    </StackPanel>
  </ScrollViewer>

</UserControl>
