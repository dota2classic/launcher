<UserControl xmlns="https://github.com/avaloniaui"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:vm="using:d2c_launcher.ViewModels"
            xmlns:m="using:d2c_launcher.Models"
            xmlns:util="using:d2c_launcher.Util"
            xmlns:asyncImageLoader="clr-namespace:AsyncImageLoader;assembly=AsyncImageLoader.Avalonia"
            xmlns:res="clr-namespace:d2c_launcher.Resources"
            x:Class="d2c_launcher.Views.Components.AcceptGameModal"
            x:DataType="vm:RoomViewModel">
   <UserControl.Resources>
       <util:InverseBoolConverter x:Key="InverseBoolConverter"/>
   </UserControl.Resources>

   <Panel IsVisible="{Binding IsAcceptGameModalOpen}" ZIndex="200"
          HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="#90000000">
       <Border HorizontalAlignment="Center" VerticalAlignment="Center"
               Padding="24" CornerRadius="0"
               Background="#121315" BorderBrush="#2d3842" BorderThickness="1"
               BoxShadow="0 12 28 0 #80000000">
           <StackPanel Spacing="16">
               <StackPanel Spacing="4" HorizontalAlignment="Center">
                   <TextBlock Text="{x:Static res:Strings.GameFound}" FontSize="{DynamicResource FontSize3XL}" FontWeight="Bold"
                              HorizontalAlignment="Center" Foreground="#D9D9D9"/>
                   <TextBlock Text="{Binding RoomModeText}" HorizontalAlignment="Center"
                              Foreground="#AAAAAA" FontSize="{DynamicResource FontSizeMD}"/>
               </StackPanel>

               <ItemsControl ItemsSource="{Binding RoomPlayers}">
                   <ItemsControl.ItemsPanel>
                       <ItemsPanelTemplate>
                           <StackPanel HorizontalAlignment="Center" Orientation="Horizontal"/>
                       </ItemsPanelTemplate>
                   </ItemsControl.ItemsPanel>
                   <ItemsControl.ItemTemplate>
                       <DataTemplate x:DataType="m:RoomPlayerView">
                           <Border Margin="3,3" Width="52" Height="52"
                                   BorderBrush="{Binding StateColor}" BorderThickness="3"
                                   CornerRadius="0" ClipToBounds="True"
                                   Background="#242c34">
                               <Panel>
                                   <TextBlock Text="{Binding Initials}"
                                              HorizontalAlignment="Center" VerticalAlignment="Center"
                                              FontSize="{DynamicResource FontSizeMD}" FontWeight="SemiBold" Foreground="#D9D9D9"/>
                                   <Image Stretch="UniformToFill"
                                          asyncImageLoader:ImageLoader.Source="{Binding AvatarUrl}"/>
                               </Panel>
                           </Border>
                       </DataTemplate>
                   </ItemsControl.ItemTemplate>
               </ItemsControl>

               <!-- Server searching state -->
               <TextBlock Text="{x:Static res:Strings.SearchingGameServer}" HorizontalAlignment="Center"
                          Foreground="#AAAAAA" FontSize="{DynamicResource FontSizeMD}"
                          IsVisible="{Binding IsServerSearchingModalOpen}"/>

               <!-- Accept/decline state (hidden while searching) -->
               <StackPanel IsVisible="{Binding IsServerSearchingModalOpen, Converter={StaticResource InverseBoolConverter}}">
                   <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="12"
                               IsVisible="{Binding HasMyPlayerResponded, Converter={StaticResource InverseBoolConverter}}">
                       <Button Classes="PrimaryButton"
                               Content="{x:Static res:Strings.Accept}" Command="{Binding AcceptGameCommand}"
                               Padding="28,10" CornerRadius="0"/>
                       <Button Classes="DangerButton"
                               Content="{x:Static res:Strings.Decline}" Command="{Binding DeclineGameCommand}"
                               Padding="28,10" CornerRadius="0"/>
                   </StackPanel>

                   <TextBlock Text="{x:Static res:Strings.WaitingForPlayers}" HorizontalAlignment="Center"
                              Foreground="#AAAAAA" FontSize="{DynamicResource FontSizeMD}"
                              IsVisible="{Binding HasMyPlayerAccepted}"/>
               </StackPanel>
           </StackPanel>
       </Border>
   </Panel>
</UserControl>
