---
name: improve-skill
description: Apply retrospective improvements to shared repo workflows or agent instructions. Use when the user wants to improve a workflow, apply a skill retrospective, or update command instructions after process feedback.
---

Follow `AGENTS.md` and the repository memory bank first.

Then read and apply the shared workflow in `.agents/commands/improve-skill.md`.

Treat `.agents/commands/improve-skill.md` as the canonical procedure unless it conflicts with higher-priority Codex instructions.
